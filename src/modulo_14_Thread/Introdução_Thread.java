package modulo_14_Thread;

public class Introdução_Thread {
/* o que são threads: são processos paralelos de dados, e liberar o usuário
 * para continuar sua tarefa.
 * 
 * as thread estão presentes o tempo todo, ter varios app rodando ao mesmo
 * tempo.
 * 
 *   corpo de uma thread:
 *   
 *      new Thread(){
 *         public void run(){ // ja é um metodo propio da Thread
 *            dentro do run fica o código da rotina executa o que nós queremos.
 *         }
 *      }.start(); o Start liga a thread que fica processando paralelamente por trás.
 * 
 * */
}
